on damage:
    if victim is a player:
        cancel event
        kill the attacker

command /fly:
	trigger:
		set player's flight mode to true

command /exterminate:
	trigger:
		kill all entities
	
	
